import {expect,test} from "@playwright/test";
const user={id:"11111111-1111-4111-8111-111111111111"};
test.describe("meal calendar",()=>{test.beforeEach(async({page})=>{await page.route("**/api/meals**",async route=>{if(route.request().method()==="GET")await route.fulfill({json:{entries:[],next_cursor:null,has_more:false}});else{const b=route.request().postDataJSON();await route.fulfill({json:{client_mutation_id:b.client_mutation_id,status:"applied",code:"OK",result:{id:b.meal_entry_id,user_id:user.id,...b.payload,version:1,created_at:new Date().toISOString(),updated_at:new Date().toISOString(),deleted_at:null},conflicts:[]}})}})});
 test("month navigation and custom meal are keyboard accessible",async({page})=>{await page.goto("/app/calendar");await expect(page.getByRole("heading",{name:"Comidas"})).toBeVisible();await page.getByRole("button",{name:"Añadir comida"}).click();await page.getByLabel("Nombre").fill("Cena histórica");await page.getByRole("button",{name:"Guardar"}).click();await expect(page.getByRole("status")).toContainText(/pendiente|Sincronizado/);await page.getByRole("button",{name:"Mes siguiente"}).press("Enter")});
 test("offline empty state is explicit and recoverable",async({page,context})=>{await context.setOffline(true);await page.goto("/app/calendar");await expect(page.getByText("Sin conexión; aún no hay datos guardados")).toBeVisible();await expect(page.getByRole("button",{name:"Reintentar"})).toBeVisible()})});
