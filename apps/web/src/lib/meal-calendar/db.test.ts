import {describe,expect,it} from "vitest";import {compactMutations} from "./db";import type {MealMutation} from "./types";
const m=(operation:MealMutation["operation"],id:string,state:MealMutation["state"]="pending"):MealMutation=>({clientMutationId:id,userId:"u",operation,mealEntryId:"e",expectedVersion:operation==="create"?null:1,payload:operation==="delete"?null:{} as never,state,createdAt:id});
describe("meal outbox",()=>{it("compacts unsent create/update",()=>expect(compactMutations([m("create","1")],m("update","2"))).toHaveLength(1));it("removes an unsent create followed by delete",()=>expect(compactMutations([m("create","1")],m("delete","2"))).toEqual([]));it("keeps independent delete after uncertain create",()=>expect(compactMutations([m("create","1","uncertain")],m("delete","2"))).toHaveLength(2));it("preserves causal order",()=>expect(compactMutations([m("update","1","sending")],m("delete","2")).map(x=>x.operation)).toEqual(["update","delete"]))});
