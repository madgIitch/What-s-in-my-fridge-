import {redirect} from "next/navigation";import {MealCalendarApp} from "@/components/meal-calendar/meal-calendar-app";import {createServerSupabaseClient} from "@/lib/supabase/server";
export default async function CalendarPage({searchParams}:{searchParams:Promise<{edit?:string;new?:string}>}){const s=await createServerSupabaseClient(),{data:{user}}=await s.auth.getUser();if(!user)redirect("/login?error=session_expired&returnTo=%2Fapp%2Fcalendar");const params=await searchParams;return <MealCalendarApp userId={user.id} initialEditId={params.edit} initialNew={params.new==="1"}/>}
