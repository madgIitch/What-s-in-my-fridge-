import {lookup} from "node:dns/promises"; import {isIP} from "node:net";
const blockedHosts=new Set(["localhost","metadata.google.internal","metadata.goog"]);
function blockedIp(ip:string){if(!isIP(ip))return true;if(ip.includes(":")){const x=ip.toLowerCase();return x==="::1"||x==="::"||x.startsWith("fe80:")||x.startsWith("fc")||x.startsWith("fd")||x.startsWith("::ffff:127.")||x.startsWith("::ffff:10.")||x.startsWith("::ffff:192.168.");}const [a,b]=ip.split(".").map(Number);return a===0||a===10||a===127||a>=224||(a===169&&b===254)||(a===172&&b>=16&&b<=31)||(a===192&&b===168)||(a===100&&b>=64&&b<=127);}
export async function assertPublicUrl(raw:string,resolver=lookup){const url=new URL(raw);if(url.protocol!=="https:"&&url.protocol!=="http:")throw new Error("URL_BLOCKED");if(url.username||url.password||blockedHosts.has(url.hostname.toLowerCase())||(isIP(url.hostname)&&blockedIp(url.hostname)))throw new Error("URL_BLOCKED");const addresses=await resolver(url.hostname,{all:true,verbatim:true});if(!addresses.length||addresses.some(a=>blockedIp(a.address)))throw new Error("URL_BLOCKED");return url;}
export async function safeFetch(raw:string,init:RequestInit={},resolver=lookup,maxRedirects=4):Promise<Response>{let current=raw;for(let i=0;i<=maxRedirects;i++){await assertPublicUrl(current,resolver);const response=await fetch(current,{...init,redirect:"manual",signal:init.signal??AbortSignal.timeout(20_000)});if(![301,302,303,307,308].includes(response.status))return response;const location=response.headers.get("location");if(!location)throw new Error("DOWNLOAD_FAILED");current=new URL(location,current).toString();}throw new Error("TOO_MANY_REDIRECTS");}
