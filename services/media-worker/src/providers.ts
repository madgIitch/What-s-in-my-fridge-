import type{RecipeExtractionProvider,SourceType,TranscriptionProvider}from"./contracts.js";
async function providerFetch(url:string,body:unknown){const response=await fetch(url,{method:"POST",headers:{"Content-Type":"application/json",...(process.env.INTERNAL_SERVICE_TOKEN?{Authorization:`Bearer ${process.env.INTERNAL_SERVICE_TOKEN}`}:{})},body:JSON.stringify(body),signal:AbortSignal.timeout(120_000)});if(!response.ok)throw new Error("PROVIDER_UNAVAILABLE");return response.json() as Promise<Record<string,unknown>>;}
export class WhisperProvider implements TranscriptionProvider{constructor(private endpoint:string){}async transcribe(audioPath:string){const result=await providerFetch(this.endpoint,{audioPath,format:"wav",sampleRate:16000,channels:1});if(typeof result.text!=="string")throw new Error("TRANSCRIPTION_INVALID");return{text:result.text,provider:"whisper-service"};}}
export class OllamaRecipeProvider implements RecipeExtractionProvider{constructor(private endpoint:string){}async extract(text:string,context:{sourceType:SourceType;sourceUrl?:string;repair?:boolean}){const result=await providerFetch(this.endpoint,{model:process.env.OLLAMA_RECIPE_MODEL??"recipe-extractor",format:"json",schemaVersion:"recipe-v1",text,context});return result.recipe??result;}}
